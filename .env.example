# ── Deployer wallet (NEVER commit the real .env file) ──
DEPLOYER_PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# ── Polygon Amoy testnet RPC ──
AMOY_RPC_URL=https://rpc-amoy.polygon.technology

# ── Polygonscan API key (for contract verification) ──
POLYGONSCAN_API_KEY=

# ── TruthToken deployed address (filled after deploy step 1) ──
TRUTH_TOKEN_ADDRESS=

# ── TruthRegistry deployed address (filled after deploy step 2) ──
TRUTH_REGISTRY_ADDRESS=

# ── CrowdedSea BountyPool address (for cross-protocol integration) ──
BOUNTYPOOL_ADDRESS=
